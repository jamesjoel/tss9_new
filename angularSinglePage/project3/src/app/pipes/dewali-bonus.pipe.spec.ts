import { DewaliBonusPipe } from './dewali-bonus.pipe';

describe('DewaliBonusPipe', () => {
  it('create an instance', () => {
    const pipe = new DewaliBonusPipe();
    expect(pipe).toBeTruthy();
  });
});
